#include<iostream>
using namespace std;
int main(){
   /* int array[]={1,2,3,4};
   // array[0]={1};
    cout<<array[2]<<endl;
    cout<<sizeof(array)<<endl;//it will print the space taken by the whole array in memory
    
    cout<<sizeof(array[0])<<endl;//it will print the space taken by the element in memory
    cout<<array<<endl;//it will print the address of array
    for (int i=0; i<(sizeof(array)/sizeof(array[0])); i++){
        cout<<array[i]<<endl;
    }
    for (int ele:array){
        cout<<ele<<endl;
    }*/
    
        
    
    return 0;
}